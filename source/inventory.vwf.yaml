---
extends: http://vwf.example.com/node3.vwf
properties:
  capacity: 
    set: |
      if ( !isNaN( value ) ) {
        value = Math.round( value );
        value = Math.max( value, 0 );
        this.capacity = value;
      }
    value: 1
  slots: []
  inventoryIsVisible: true
methods:
  add:
  swap:
  remove:
children: 
scripts: 
- source: source/inventory.js
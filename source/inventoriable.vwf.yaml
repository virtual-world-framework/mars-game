---
properties:
  isInventoriable: true
  iconSrc: ""
methods:
  grab:
events:
  pickedUp:
  dropped:
  pointerDown:
  grabbed:
scripts: 
- |

  this.pointerDown = function( pointerInfo, pickInfo ) {

    // TODO: Find player rover and get its distance from this object
    if ( pickInfo.distance < 5 ) {

      var pos = { "x": pointerInfo.screenPosition[0], "y": pointerInfo.screenPosition[1] };
      this.grab( pos, this.client );

    }

  }

  this.grab = function( position, client ) {

    if ( !client ) {
      client = this.client;
    }

  	this.grabbed( client, this.iconSrc, position, this.parent.name );

  }

  //@ sourceURL=source/inventoriable.vwf